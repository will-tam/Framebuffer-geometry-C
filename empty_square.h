#ifndef _EMPTY_SQUARE_H
#define _EMPTY_SQUARE_H

/* 
  Just draw a non-paint rectangle,
  from point((fb.vinfo.xres / 2) + 100; (fb.vinfo.yres / 2) + 100) to point((fb.vinfo.xres / 2) - 100 ; (fb.vinfo.yres / 2) - 100).
  Second example to show how to display picture on FB.
*/
void empty_square();

#endif